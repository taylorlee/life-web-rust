
runs out of memory >dim=50
    only in asm.js
    can I pass emscripten arg?

wasm slower than asm

would be very nice:
    faster compiles
    hot reload
    html! to statically check xml validity eg <p></div>
